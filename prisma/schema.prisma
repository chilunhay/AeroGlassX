datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client-js"
}

model PreOrder {
  id                 String   @id @default(uuid())
  name               String?
  email              String
  phoneNumber        String
  createdAt          DateTime @default(now())
}
